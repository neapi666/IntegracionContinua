# safelist
language: java
sudo: true
script: ant test
services:
  - mysql
branches:
  only:
  - master

before_install:
  - mysql -e 'CREATE DATABASE sistemadb;'
  - mysql -u root -p sistemadb < .\sistemadb.sql
install: ant deps

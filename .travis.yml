# safelist
language: java
sudo: true
script: ant test
services:
  - mysql
branches:
  only:
  - master

before_install:
  - mysql -e 'CREATE DATABASE sistemadb;'
  - mysql -u root sistemadb < bd/sistemadb.sql
install: ant deps
